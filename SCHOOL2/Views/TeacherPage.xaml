<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="SCHOOL2.Views.TeacherPage"
			 Title="TeacherPage">
    <StackLayout>
        <Label Text="Teachers" FontSize="Large" HorizontalOptions="Center" Margin="0,10,0,10" />
        <StackLayout>
            <Entry x:Name="entryFirstName" Placeholder="Prénom" />
            <Entry x:Name="entryLastName" Placeholder="Nom" />
            <Entry x:Name="entrySalary" Placeholder="Salaire" />

            <Button Text="Ajouter Enseignant" Clicked="OnAddTeacherClicked" />
        </StackLayout>

        <CollectionView x:Name="teacherCollection"
					    ItemsSource="{Binding AllTeachers}"
					    Margin="20">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout>
                        <Label Text="{Binding DisplayName}" FontSize="Medium" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
    </StackLayout>
</ContentPage>